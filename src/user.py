class User(object):
    """docstring for User."""
    def __init__(self, data):
        """Takes `data` as the MongoDB entry of the Stripe customer"""
        super(User, self).__init__()
        self.arg = arg
